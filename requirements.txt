# Core MCP dependencies
anthropic>=0.40.0
mcp>=1.0.0

# GUI control
pyautogui>=0.9.54
mss>=9.0.0
Pillow>=10.0.0

# Windows UI Automation (highly recommended - much more reliable!)
pywinauto>=0.6.8

# Optional: OCR text detection (GPU-accelerated)
# easyocr>=1.7.0

# Optional: Florence-2 visual grounding (requires GPU, ~2GB VRAM)
# transformers>=4.40.0
# accelerate>=0.25.0
# torch>=2.0.0
